export interface Ipepole {
  id: string;
  name: string;
}

export interface Igrade {
  student: string;
  grade: string;
}

export interface Itask {
  id: string;
  title: string;
  description: string;
  status: boolean;
}
